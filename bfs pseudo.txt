#useful website http://tristanpenman.com/demos/n-puzzle/

def calculate_bfs(board)
    done = False
    openList = []
    closedList = []

    temp = board

    while Not Done
        if(temp == Board.completed_Board):
            Done = True
        else:
            closedList += temp
            openList += temp.calculate_child_states()
            openList = [x for x in openList if x not in closedList] #removes lists that have moves already done
            temp = openList.pop(0)
